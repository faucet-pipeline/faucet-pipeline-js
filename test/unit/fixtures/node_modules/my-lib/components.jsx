import createElement from "./elements";

export function Button({ type, label }) {
	return <button type={type}>{label}</button>;
}

export function List(_, ...children) {
	return <ul>
		{children.map(item => (
			<li>{item}</li>
		))}
	</ul>;
}
